<UserControl x:Class="AdventurersGuild.UI.Views.Quests.QuestEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:AdventurersGuild.ViewModels.Quests;assembly=AdventurersGuild.ViewModels"
             xmlns:converters="clr-namespace:AdventurersGuild.UI.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:QuestEditViewModel}">

    <UserControl.Resources>
        
        <converters:EnumDescriptionConverter x:Key="EnumDescriptionConverter" />
        
        <Style x:Key="FormTitle" TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style x:Key="FormPanel" TargetType="StackPanel">
            <Setter Property="Margin" Value="8" />
        </Style>
        <Style x:Key="FieldLabel" TargetType="Label" BasedOn="{StaticResource {x:Type Label}}">
            <Setter Property="Margin" Value="0,8,0,2" />
        </Style>
        <Style x:Key="FormButtons" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal" />
            <Setter Property="Margin" Value="0,16,0,0" />
        </Style>
        <Style x:Key="FormButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Margin" Value="0,0,4,0" />
            <Setter Property="Padding" Value="16,4" />
        </Style>
    </UserControl.Resources>

    <StackPanel Style="{StaticResource FormPanel}">
        <TextBlock Text="{Binding FormTitle}" Style="{StaticResource FormTitle}" />

        <Label Content="Название" Style="{StaticResource FieldLabel}" />
        <!-- TODO: привязать к свойству Title. Используйте UpdateSourceTrigger=PropertyChanged,
             чтобы значение обновлялось при каждом нажатии клавиши, а не при потере фокуса -->
        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="Описание" Style="{StaticResource FieldLabel}" />
        <TextBox Height="80" TextWrapping="Wrap" AcceptsReturn="True"
                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/> <!-- TODO: привязать к свойству Description -->

        <Label Content="Награда" Style="{StaticResource FieldLabel}" />
        <TextBox Text="{Binding Reward,  UpdateSourceTrigger=PropertyChanged}"/> <!-- TODO: привязать к свойству Reward -->

        <Label Content="Статус" Style="{StaticResource FieldLabel}" />
        <ComboBox ItemsSource="{Binding Statuses}"
                  SelectedItem="{Binding Status}"> <!-- TODO: привязать SelectedItem к свойству Status -->
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        
        <Label Content="Исполнитель" Style="{StaticResource FieldLabel}" />
        <ComboBox ItemsSource="{Binding AdventurerOptions}"
                  SelectedItem="{Binding SelectedAdventurerOption}"
                  Style="{StaticResource FormComboBox}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock>
                        <TextBlock.Text>
                            <Binding Path="Value.Name" 
                                     TargetNullValue="Не назначен" 
                                     FallbackValue="Не назначен" />
                        </TextBlock.Text>
                    </TextBlock>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <StackPanel Style="{StaticResource FormButtons}">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Style="{StaticResource FormButton}" />
            <Button Content="Отмена" Command="{Binding CancelCommand}" Style="{StaticResource FormButton}" />
        </StackPanel>
    </StackPanel>
</UserControl>
